pipeline {
    agent {
        label 'Mydocker'
    }

    stages {
        stage('Get-Node') { 
            agent {    
                docker {
                    label 'Mydocker'
                    image 'node:8-alpine'
                    args '-p 3000:3000'
                }
            }    
            steps {
                echo "Node built"
            }    
        }
    
        stage('Build') { 
            steps {
                sh 'npm run build' 
            }
        }
    }
}