pipeline {
    agent{
        docker {
            image 'node:carbon-alpine'
            args '-p 3000:3000'
            withRun '-u root'
        }
    }

    stages {
        stage('Build') { 
            steps {
                sh 'npm run build' 
            }
        }
    }
}