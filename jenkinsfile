pipeline {
    agent{
        label 'docker'
        docker {
            label 'docker'
            image 'node:8-alpine'
            args '-p 3000:3000'
        }
    }

    stages {
        stage('Build') { 
            steps {
                sh 'npm run build' 
            }
        }
    }
}