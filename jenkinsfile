pipeline {
    agent {
        label 'dockerserver'
        docker {
            label 'dockerserver'
            image 'node:8-alpine'
            args '-p 3000:3000'
                }    
        }
            

    stages {
        stage('Build') { 
            steps {
                sh 'npm run build' 
            }
        }
    }
}